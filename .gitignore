#Local terraform directories
**/.terraform/*

# Terraform Lockfile
.terraform.lock.hcl

# tfstate files
*.tfstate
*.tfstate.*

# crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data,
# password, private keys, and other secrets. These should not be pary of version control as they are data points which are potentially sensitive and subject to change dependingon the enviroment
*.tfvars

override.tf
override.tf.json
*_override.tf
*_override.tf.json

# ignore CLI configuration files
.terraformrc
terraform.rc